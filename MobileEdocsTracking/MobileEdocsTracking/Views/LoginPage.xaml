<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
               xmlns:local="clr-namespace:MobileEdocsTracking"
             x:Class="MobileEdocsTracking.Views.LoginPage">
    
    <ContentPage.Content>
        <Grid Margin="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <StackLayout Grid.Row="0">
            <Image Source="logo_black_v2" HeightRequest="50" WidthRequest="50"/>
            <Label Text="e-Docs Usa Tracking System"                 
                 HorizontalOptions="Center"/>
                <Entry x:Name="UsernameEntry" 
                       Text="{Binding Username}"
                       Placeholder="Username" />
                <Entry x:Name="PasswordEntry"
                       Text="{Binding Password}"
                       Placeholder="Password"
                       IsPassword="True" />
                <Label Text="Invalid credentials entered" 
                       IsVisible="{Binding AreCredentialsInvalid}" 
                       TextColor="Red" />

            </StackLayout>
            <Button Text="Login In" Grid.Row="2"  Command="{Binding AuthenticateCommand}"  HorizontalOptions="Center" BackgroundColor="GhostWhite"/>
            </Grid>
    </ContentPage.Content>
</ContentPage>