<Page x:Class="AlanoClubInventory.Views.AddEditDeleteCategoriesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:AlanoClubInventory.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
        xmlns:vm="clr-namespace:AlanoClubInventory.ViewModels"
       d:DataContext="{d:DesignInstance Type=vm:AddEditDeleteCategoriesViewModel}"
        Background="{DynamicResource ControlFillColorDefault}"
      
      Title="Add Edit Delete Categories">
    <ScrollViewer VerticalScrollBarVisibility="Auto"
                  HorizontalScrollBarVisibility="Auto"
                  Padding="10">
        <StackPanel VerticalAlignment="Top" HorizontalAlignment="Center">
            <DockPanel Background="{DynamicResource MenuItems}"  Cursor="Hand" >
                <MenuItem Header="Prices"  Click="Prices" FontSize="12" />
                <MenuItem Header="Inventory"  Click="InventoryPage" FontSize="12">
                    <MenuItem Header="Receipts"  Click="DailyRecpts" Cursor="Hand"/>
                </MenuItem>
                <MenuItem Header="Prices">
                    <MenuItem Header="Club Prices" Click="AddEditProductPrices" Cursor="Hand"/>
                    <MenuItem Header="Other Products"  Click="OtherProd" FontSize="12" />
                <MenuItem Header="Daily Till"  Click="Till"  FontSize="12"/>
                <MenuItem Header="Reports"  Click="Reports"  FontSize="12"/>
                <MenuItem Header="Reload Page"  Click="Categories" FontSize="12" />
                <MenuItem Header="Go Back"  Click="GoBack"  FontSize="12"/>


            </DockPanel>

            <Grid  VerticalAlignment="Top" HorizontalAlignment="Center" >
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <!-- Title -->
                    <RowDefinition Height="Auto"/>
                    <!-- Main content -->
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <!-- Fits the text -->
                    <ColumnDefinition Width="*"/>
                    <!-- Fills remaining space -->
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="textBlock" Grid.Column="0" HorizontalAlignment="Center" Margin="0,10,0,0" TextWrapping="Wrap" Text="Add Edit Categories" VerticalAlignment="Top"/>
                <DataGrid     Grid.Column="0" x:Name="dataGrid" Grid.Row="1"  AutoGenerateColumns="False" HorizontalAlignment="Left" VerticalAlignment="Top" 
                  ItemsSource="{Binding Categories}" Margin="24,17,0,0" SelectedItem="{Binding SelectedItem, Mode=TwoWay}"  
                  SelectionChanged="dataGrid_SelectionChanged"  
                  GridLinesVisibility="All" SelectionUnit="FullRow"  IsReadOnly="False" CanUserAddRows="True" CanUserSortColumns="True" Padding="10" CanUserDeleteRows="False"  SelectionMode="Extended"  LostFocus="dataGrid_LostFocus" >
                    <DataGrid.Resources>
                        <Style TargetType="DataGridRow">
                            <Style.Triggers>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="LightGoldenrodYellow"/>
                                    <Setter Property="Foreground" Value="Black"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.Resources>
                    <DataGrid.RowStyleSelector>
                        <StyleSelector/>

                    </DataGrid.RowStyleSelector>
                    <DataGrid.CellStyle>
                        <Style TargetType="DataGridCell">
                            <Setter Property="Padding" Value="10"/>
                            <Setter Property="Margin" Value="10"/>
                        </Style>
                    </DataGrid.CellStyle>
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="id" Binding="{Binding ID}" Visibility="Hidden" Width="SizeToCells"/>
                        <DataGridTextColumn Header="Name" Binding="{Binding CategoryName}"  Width="SizeToCells" />
                        <DataGridTemplateColumn Header="Bar Item">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <CheckBox IsChecked="{Binding BarItem, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        

                    
                           
                        <DataGridTemplateColumn Width="SizeToCells">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="Add" Command="{Binding DataContext.AddCategory,RelativeSource={RelativeSource AncestorType=DataGrid}}" Background="LightBlue"
                                            CommandParameter="{Binding ElementName=dataGrid}"
                                           Foreground="Black" BorderBrush="DarkBlue"  BorderThickness="2"
                Cursor="Hand"  />
                                    <!-- ElementName=dataGrid RelativeSource={RelativeSource FindAncestor, AncestorType=Window}-->
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Width="SizeToCells">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="Update" Background="LightYellow"
                                    Command="{Binding DataContext.EditCategory,RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                            CommandParameter="{Binding ElementName=dataGrid}"  Foreground="Black"  BorderBrush="DarkBlue"  BorderThickness="2"
                Cursor="Hand"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Width="SizeToCells">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="Delete" Background="Red"
                           Command="{Binding DataContext.DelCategory,RelativeSource={RelativeSource AncestorType=DataGrid}}"
         CommandParameter="{Binding ElementName=dataGrid}"  Foreground="Black"  BorderBrush="DarkBlue"  BorderThickness="2"
                Cursor="Hand"/>


                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>



            </Grid>
          
        </StackPanel>
        
    </ScrollViewer>
    <!--  SelectedItem="{Binding SelectedMood, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" -->
</Page>
