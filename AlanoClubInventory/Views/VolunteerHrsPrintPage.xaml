<Window x:Class="AlanoClubInventory.Views.VolunteerHrsPrintPage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AlanoClubInventory.Views"
        xmlns:vm="clr-namespace:AlanoClubInventory.Utilites"
        mc:Ignorable="d"
        Title="VolunteerHrsPrintPage" Height="450" Width="800"
           VerticalAlignment="Top"
        Background="{StaticResource ControlFillColorDefault}">
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <StackPanel Grid.Row="0" Orientation="Horizontal" VerticalAlignment="Top" HorizontalAlignment="Center">
                <TextBlock      Margin="5" TextWrapping="Wrap" Text="{Binding TxtTitle}"  Padding="10"  HorizontalAlignment="Center"  FontSize="14" VerticalAlignment="Center"/>

            </StackPanel>
            <StackPanel Grid.Row="1" VerticalAlignment="Top"  HorizontalAlignment="Center"  Orientation="Vertical"  Visibility="{Binding IsProgressBar, Converter={StaticResource BoolToVisibilityConverter}}" 
                            >
                <ProgressBar Minimum="0" Maximum="1000" Height="30" Margin="20" VerticalAlignment="Center"  HorizontalAlignment="Center"
Value="{Binding ProgressValue, Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"  Width="150"    >
                    <ProgressBar.Foreground>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                            <GradientStop Color="LimeGreen" Offset="0"/>
                            <GradientStop Color="DarkGreen" Offset="1"/>
                        </LinearGradientBrush>
                    </ProgressBar.Foreground>
                </ProgressBar>
            </StackPanel>
            <StackPanel Grid.Row="3" Grid.Column="0" VerticalAlignment="Center" HorizontalAlignment="Center" 
               Width="1000">
                <RichTextBox  Name="ReportRichTextBox" IsReadOnly="True" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" Margin="0,0,0,0"   vm:RichTextBoxHelper.BoundDocument="{Binding ReportContent}"
                   VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
                    <!-- <FlowDocument>
              <Paragraph>
                  <Run Text="{Binding ReportContent}"/>
              </Paragraph>
          </FlowDocument>
          -->
                </RichTextBox>
            </StackPanel>
            <StackPanel Grid.Row="5" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
                <Button Content="Print Report"  Command="{Binding  PrintReportCmd}"  Background="Ivory" Margin="5" Cursor="Hand"
            Foreground="Black"  BorderBrush="DarkBlue"  BorderThickness="2" VerticalAlignment="Center" HorizontalAlignment="Center" Width="200" />
                
                <Button Content="Exit"  Margin="5" Width="200" Cursor="Hand" Background="Red"  Foreground="Black"  BorderBrush="DarkBlue" 
                        BorderThickness="2" VerticalAlignment="Center" HorizontalAlignment="Center"     Click="Button_Click"/>
            </StackPanel>

        </Grid>
    </ScrollViewer>
</Window>
