ffmpeg -i video.flv image%d.jpg split viedo into images
ffmpeg -f image2 -i image%d.jpg imagestovideo.mpg  Convert images into a video
Output one image every second:

ffmpeg -i input.mp4 -vf fps=1 out%d.png
Output one image every minute:

ffmpeg -i test.mp4 -vf fps=1/60 thumb%04d.png
Output one image every 10 minutes:

ffmpeg -i test.mp4 -vf fps=1/600 thumb%04d.png

Extract one picture each 50 frames:

thumbnail=50
Complete example of a thumbnail creation with ffmpeg:

ffmpeg -i in.avi -vf thumbnail,scale=300:200 -frames:v 1 out.png

ffmpeg -i video.mpg -r 1 -f image2 ffmpeg_temp/%05d.png


ffmpeg.exe  -i l:\imagestovideo.mp4 -r 1/5 -start_number 0  D:\Archives\ScanQuireSavedImages\ImagesVieodConverted\imagestovideo%d.png
ffmpeg -framerate 30 -pattern_type glob -i '*.png' \
  -c:v libx264 -pix_fmt yuv420p out.mp4

ffmpeg.exe  -i l:\imagestovideo.mp4 -r 1 -f image2  D:\Archives\ScanQuireSavedImages\ImagesVieodConverted\imagestovideo%d.png

-i "l:\imagestovideo.mp4" -codec:v png -pix_fmt yuv420p -vframes 3 -ss 00:00:00.0200000 -s 1920x1080 "l:\imagestovideo_screenshot.png"

-v quiet -print_format json -show_streams -i "l:\imagestovideo.mp4"

ffmpeg.exe  -i l:\imagestovideo.mp4 -r 1 -s 1920x1080   -sample_fmt s32p  D:\Archives\ScanQuireSavedImages\ImagesVieodConverted\imagestovideo%d.png