using System;

namespace Scanquire.Public
{
    /// <summary>Empty interface definition of some command to apply to a document, file, image, page, etc.</summary>
    public interface ISQCommand
    { }
}
